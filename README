This is a simple Java project to read/parse order details and write it on the console. The accepted file formats are:
1) .csv
2) .json

This application reads file from the classpath
This application is build using :
1) Java
2) Spring boot
3) opencsv
4) json-simple

In order to run this application, please put your file in resource folder and do a maven build.
Then it can be run via console using below command :
java -jar IncomeDataParser-22.11.0-SNAPSHOT.jar <fileName.format>